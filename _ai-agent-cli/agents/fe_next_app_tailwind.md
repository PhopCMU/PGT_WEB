# Agent FE1 — Next.js App Router + Tailwind CSS v4

## Primary responsibilities
- Implement UI changes in App Router
- Tailwind v4 styling consistent
- Add version display (prefer server component reads package.json)

## Version display
- Create helper to read package.json.version (server-only)
- Render version in Footer (default) unless user specifies otherwise