# Agent FE2 — React SPA + axios 1.14.0

## Primary responsibilities
- Implement UI changes with React + axios patterns
- Provide typed API calls and error/loading states
- Add version display by calling backend GET /version

## Version display
- axios instance with baseURL from env
- VersionBadge component with error fallback